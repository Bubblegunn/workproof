# Roadmap

Three next steps, in no particular order. Open an issue if you want to take one.

- A `compare` command that puts two authors side by side in one report
- A GitHub Action that attaches a workproof summary to a release
- A hosted verifier page that reads a report JSON and explains each figure

Things deliberately not planned: anything that needs a server, a hosted account, or reads code content.
